# Property scope

Record only verified project facts here. Private facts may remain in access-controlled storage; redact them from public
copies as required by `sources/README.md`. Use the non-validated Markdown tokens `entity:room-kitchen` and
`source:original-plan-2024` as the minimal convention for linking a model-specific claim to a stable entity and its
provenance record. This starter deliberately has no property facts.
